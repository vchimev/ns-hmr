module.exports.reload = function ({ type, resource }) {
    return `
    if (module.hot) {
        module.hot.accept();
        module.hot.dispose(() => {
            global.__hmrRefresh({ type: '${type}', resource: '${resource}' });
        })
    }
`};
